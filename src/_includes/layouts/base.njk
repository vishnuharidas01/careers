<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{{ title or "Career Pages" }}</title>
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; margin: 0; padding: 24px; line-height: 1.5; }
    .wrap { max-width: 920px; margin: 0 auto; }
    .card { border: 1px solid #e5e7eb; border-radius: 16px; padding: 20px; margin: 16px 0; }
    .muted { color: #6b7280; }
    a { color: inherit; }
    h1,h2 { margin: 0 0 12px; }
  </style>
</head>
<body>
  <div class="wrap">
    {{ content | safe }}
  </div>
</body>
</html>

